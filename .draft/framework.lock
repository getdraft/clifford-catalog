schemaVersion: "1.0"
framework:
  source: https://github.com/getdraft/draftsman.git
  vendoredPath: .draft/framework
  updatePolicy: explicit
  # NOTE: the fix/inline-relationships branch we previously pinned to has
  # since been merged into main and released as part of the 0.61.0
  # changelog entry. We are now synced to main again, not a branch - the
  # one remaining gap is that main has not yet been tagged v0.61.0 (only
  # v0.60.0 exists as a tag at the time of this sync), so syncedTag is
  # still empty. See EXERCISE_REPORT.md's "Version Update" section for
  # the full history and rationale.
  syncedRef: main
  version: "0.61.0"
  syncedTag: ""
  syncedCommit: 2d25d16a7776d6a9e725408cbe3b7704ab5b0819
